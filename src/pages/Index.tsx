import React from "react";

const Index: React.FC = () => {
  return (
    <main style={{ padding: 24 }}>
      <h1>Anime Character Recognizer</h1>
      <p>Welcome — upload an image to start recognition (UI stubs are in place).</p>
    </main>
  );
};

export default Index;